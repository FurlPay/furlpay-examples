# Go — verified webhooks

Signature-verified Furlpay webhook server using `furlpay-go` and the standard library — no framework needed.

```sh
go mod tidy
FURLPAY_ENDPOINT_SECRET=whsec_... go run .
```
